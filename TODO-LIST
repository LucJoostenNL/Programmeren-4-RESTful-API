Stefan:
dubbel routing uitleg door stijn in WA groep.

DELETE      -   /api/appartments/:id/reservations/:id

luc:
RETURN SUCCES AFTER LOG IN
API TESTS

samen:
Prepared statements
invullen enviroment details voor DB connection (Heroku/gitlab)?
Comments, Comments en nog is comments!